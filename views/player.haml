%h2= @player.name

%span.label.label-success= "#{@wins} wins"
%span.label.label-important= "#{@losses} losses"

.container
%h3 Recently

%table.table.table-condensed.table-striped
  %tr
    %th Date
    %th Against
    %th Result
  - @recent.each do |result|
    %tr
      %td= result.time
      %td= result.opponent(@player).display_name
      %td= result.result(@player)
